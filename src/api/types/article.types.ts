export type TParams = {
  id: string
}

export type TRecords = {
  id: number,
  is_delete: number,
  desc: string,
  title: string,
  html: string,
  markdown: string,
  create_user: string,
  update_user: string,
  created_at: string,
  updated_at: string,
}